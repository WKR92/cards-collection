const page = () => {
  return (
    <div className="flex justify-center items-center w-screen h-screen">
      <p className="text-white">routing via build-in Link component</p>
    </div>
  );
};

export default page;
